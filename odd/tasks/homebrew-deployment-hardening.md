# Homebrew Deployment Hardening

## Objective

Build a same-repository, Homebrew-only deployment component that can inspect and plan a bounded gateway installation change, recover from interrupted local operations, and keep activation separate and operator-controlled. The Raspberry Pi remains on verified v0.6.5, stopped, until a separately authorized device operation.

## Problem and why

The existing `/opt` install/update scripts and unit do not describe the Homebrew installation. Three one-off demo remediation candidates failed independent security review and must not be executed. A maintained, testable transaction contract is needed before attempting deployment hardening.

## Scope and constraints

- Homebrew profile only; leave `/opt` entry points, unit, and behavior unchanged. Do not infer paths or service manager from ambient state.
- Design explicit state, path/identity, privilege, secret-redaction, locking, crash-recovery, and rollback contracts before mutating code.
- Preserve the Raspberry v0.6.5 stopped state. No SSH, sudo, brew mutation, gateway/TUI execution, service activation, or staging of rejected remediation v1/v2/v3 in this feature's local implementation tasks.
- No release, remote deployment, push, PR, or merge is authorized by this plan.
- Isolated branch/worktree: `feat/homebrew-deployment-hardening` at `/home/jesus/Work/amg-rfid-gateway-homebrew-deployment-hardening`, branched from `origin/main` at `5df0cbe456afd5856d6aece28a017784d6082913`. Preserve the original dirty worktree.
- Treat formula-level assertions not independently verified against the tap as assumptions. Reject inconsistent inventory rather than guessing.

## Tasks

- [x] **CONTRACT-1 — Specify the Homebrew deployment safety contract.** Route: delegated exploration already mapped 4+ surfaces; bounded documentation task may be inline if one file. Record exact allowed states, targets, preconditions, mutation and rollback identities, recovery invariants, privilege separation, secret-safe evidence, and explicit non-activation. Define test scenarios and distinguish known source facts from Raspberry-specific assumptions. Check: review the contract against prior rejected findings and existing Homebrew packaging evidence; `git diff --check`. Commit a reviewable documentation work unit.
- [x] **INSPECT-1 — Implement hermetic, non-authorizing metadata inspection.** Route: delegated worker (multi-file code/tests/doc). Explicit local paths and expected metadata only; reject unsafe ancestors including sticky world-writable parents, malformed identity, symlinks, unsupported platform, and secret disclosure. Output is a snapshot, not verified Homebrew provenance or a mutation plan. Strict TDD RED/GREEN/REFACTOR plus CLI/adversarial fixture tests and focused/full Go checks. Commit code/tests/docs as one work unit.
- [x] **OBSERVE-2 — Compare local executable bytes with pinned release evidence.** Route: delegated worker (multi-file code/tests/doc). Use official v0.6.5 archive/checksum evidence to pin member digests, compare bytes through bounded no-follow reads, and report a secret-safe, deterministic NON-AUTHORIZING content observation or refusal. Do not claim Homebrew receipt authenticity, identity continuity, a change plan, or deployment readiness. Rename misleading `plan` CLI/API to observation terms; test the real production reader positive path as well as refusals. Strict TDD, independent security verification, focused/full Go checks, then coherent work-unit commit.
- [ ] **PLAN-3 — Build a real deterministic change plan.** Route: delegated read-only mapping then worker. Add explicit selected data/runtime/unit surfaces, typed data pre-state and conditional proposed transitions with defined inverses and blockers. A unit transition requires fresh independently verifiable service-manager evidence for the exact scope/unit; caller-supplied `verified` assertions and historical tracker records do not suffice. Under today's no-service-command constraint, the production unit transition must remain blocked; synthetic fixtures may test the typed contract. Bind to content observation and snapshot identities; output remains non-authorizing, cannot apply or activate. No service commands, SSH, sudo, brew, or remote/device mutation. Require strict TDD and independent verification before a separate commit.
- [ ] **TRANSACTION-1 — Implement bounded local transaction and recovery.** Route: delegated worker (multi-file code/tests). Require durable journal, exclusive lock, exact object identity, crash/fault injection coverage, idempotent recovery, and fail-closed ambiguity. Never apply to the Raspberry during this task. Independent security review and applicable tests before committing.
- [ ] **SERVICE-1 — Integrate Homebrew service boundaries without activation.** Route: delegated worker (multi-file code/tests). Make unit/service transitions explicit, keep file preparation distinct from start/enable, and prove isolation assumptions with fixtures or report them unresolved. Local-only verification and independent review; no device mutation. Commit one coherent work unit.
- [ ] **DEVICE-GATE — Prepare a read-only device validation and operator gate.** Route: delegated read-only verification. Compare the finished local tool contract with the actual stopped installation without secrets; no remote mutation. Any application, privileged invocation, or activation is a separate explicit authorization and is not a checkbox auto-continuation.

## Acceptance criteria and checks

1. Homebrew and `/opt` contracts remain separate; existing `/opt` entry points are not changed.
2. Inspect and plan disclose no config scalars or secrets, make no modifications, and reject unknown paths, symlinks, or changed identities.
3. Every mutation has a specified inverse, durable progress evidence, bounded privilege, and explicit fail-closed behavior for interrupted or ambiguous recovery.
4. Preparing files neither enables nor starts a service; any activation requires a later operator decision and evidence of local-only listeners/network confinement.
5. Fault-injection tests cover interruptions before and after each step, retries, identity replacement, wrong permissions, partial service errors, and sanitized diagnostics.
6. Required checks are recorded with observed outcomes, including failures, skips, and unavailable verification.

## Verification and delivery

- Resolved TDD: strict for Go changes, sourced from this project's previously recorded Go-change workflow; focused RED before production edits, GREEN, REFACTOR, then uncached `go test -count=1 ./...` when applicable. Documentation-only task uses diff/readback instead.
- Delivery strategy: `ask-on-risk`; initial forecast exceeds approximately 400 authored changed lines across multiple work units. User chose `stacked-to-main`: keep independent reviewable slices targeted to updated main; no push/PR is implicit.
- One Conventional Commit per completed work unit; record each commit identity and assessed review outcome here. Native review candidates are commits or PR slices, not task checkboxes.

## Progress and evidence

- Scope confirmed by user: same repository, Homebrew-only first stage; `/opt` out of scope.
- Read-only mapper identified two distinct packaging/service contracts and mismatch between legacy standalone-binary script URLs and release archives. This does not independently verify tap history.
- Initial isolated worktree created from the v0.6.5 merge commit; no Raspberry or source changes.
- User selected independent main-targeted review slices (`stacked-to-main`); this does not authorize publishing PRs.
- Contract `docs/homebrew-deployment-contract.md`: first independent challenge BLOCKED an overclaim, follow-up identified one contradictory `planned` row; both were corrected to make per-operation proofs/tests and filesystem/systemd trust gates prerequisites, not current guarantees. The original-worktree prior activation tracker was read in the follow-up; no remaining blocking overclaim was found beyond the corrected row.
- CONTRACT-1 documentation work-unit commit `0c46ae5bc21b98ec1c71cac2247927e25bca1390` contains only the contract and initial tracker (120 added lines); staged and committed-range `git diff --check` passed. Untracked `.codegraph/` is a generated index cache and was not staged. Native committed-range assessment returned `unassessable` because untracked files require an explicit declaration; no native outcome was invented. Independent read-only verifier PASS inspected both exact committed files and confirmed no recovery guarantee; documentation task complete. Later tracker progress was committed with INSPECT-1 or remains in the worktree as separately noted, not attributed to CONTRACT-1.

- Original PLAN-1 writer returned `partial`; independent verifier BLOCKED it despite passing uncached focused/full Go suites and `git diff --check`: caller-supplied inode is not package provenance, `inspection_only` is not a plan, root-owned sticky world-writable ancestors were allowed, second stat type assertion could panic, and CLI/adversarial tests were absent. No code commit or device activity.
- Replanned within the confirmed Homebrew-only scope: narrow the current work unit to INSPECT-1 (honest read-only metadata snapshot), then add PLAN-2 for independent provenance and deterministic planning. Neither task authorizes mutation or activation; no acceptance criteria were removed.
- INSPECT-1 correction independently PASS for snapshot-only behavior: focused/full uncached Go suites and staged `git diff --check` passed; 2 primary LSP checks clean and 1 timed out. Work-unit commit `6b1b7c8d789595e4eaca4faa8c1c15a4343b16ce` contains 7 inspector code/test/doc files plus the then-current tracker delta; 398 additions/4 deletions. Native committed-range assessment was `unassessable` due to untracked `.codegraph/`; required independent verification already passed. No provenance/plan/deployment claim. This later completion note is not part of that commit.
- Read-only PLAN-2 mapping found release workflow hashes tarballs but not installed binaries or individual members; historical tracker provided no actual digests. User selected official v0.6.5 archive + checksum verification as trust anchor. Byte equality to a verified member will not establish Homebrew receipt authenticity or race-free identity.
- Fetched official v0.6.5 assets into an ephemeral local directory using repository-local `git amg-gh`; both archives passed published `sha256sum -c checksums.txt`. amd64 archive `bac8c7f75fb3f5b2dfc7a235fa0fc86165f83ae3cc6e6a7efe3fa9bc5b4e959b`, gateway member `025db883cc0bd424c3c523c6425ea96adf64456b9b3e136bf31c3c280528fac5`; arm64 archive `8874cd97582b582b28fc212014d9c24f043b540a01e2e5a42f87b68c83c8578f`, gateway member `60a6acd86e65d5ccfbbf42bc27da72196ad65a1e941a8648def65a63c203d8a5`. Exact tar member extraction succeeded after an initial wrong-member failure was discarded. Local annotated tag resolves to `5df0cbe456afd5856d6aece28a017784d6082913`; temp assets were removed. Neither installed Raspberry executable nor tap history was rechecked; no device operation.
- Former PLAN-2 writer returned `partial`: pinned-digest read-only comparison and CLI `plan` command, tests/docs; focused RED/GREEN and uncached focused/full Go suites passed. First independent verifier BLOCKED missing inventory/steps, opened-handle checks, and positive/CLI tests. Correction added typed package/config inventory, blocked checklist, opened-handle checks, synthetic positive and stubbed CLI tests. Second independent verifier still BLOCKED: all proposed changes are `none`, service/data pre-state is absent, and production positive reader/CLI path is untested. It is a deterministic blocked content observation, not the promised change plan. No code commit or device action.
- User chose `split-observation` without lowering final acceptance: OBSERVE-2 is content-only and PLAN-3 retains the real change-plan requirement. `content-check`/`ContentObservation` replaced misleading `plan` command/API; real local positive production-reader and synthetic equality tests passed. Three-line superseded `docs/homebrew-deployment-plan.md` notice was accepted as a non-blocking placeholder by the independent verifier. Focused/full uncached Go checks and writer Darwin CLI cross-compile passed; independent verifier PASS explicitly limited to non-authorizing content observation. Staged `git diff --check` passed for all eight work-unit files, including formerly untracked files. Commit `6a7ec87fad864596b48e6406e2a8352bfb176f5f` has 423 additions/5 deletions; native assessment returned `unassessable` due to generated untracked `.codegraph/`, so no native outcome was claimed. No actual installed release binary or Raspberry state checked; no publication/device operation. Completion record is later tracker progress, not part of that commit.
- PLAN-3 read-only mapping found that explicit data/runtime/unit targets, typed metadata and service pre-state are absent from today's `ContentObservation`; historical stopped-state records are not current evidence. User selected `require-fresh-evidence`: refuse unit transitions without current independently verifiable service-manager evidence; a caller-provided `verified=true` or fixture cannot satisfy production proof. No service or device query was made.

- PLAN-3 initial delegated worker returned `partial`: uncommitted pure metadata proposal for existing data directory, identity-bound conditional inverse, service transition blocked, and an unexported observation seal. Initial independent verifier **BLOCKED**: mutable exported observation fields could substitute inventory; proposed modes `0500`/`0000` removed owner write; exact ordinary `0775` data pre-state was rejected. This was the previous-session handoff, not an approved plan.
- On resume, the delegated correction added a content-bound observation seal, refused non-owner-writable target modes, and narrowly allowed an exact ordinary `0775 → 0700` conditional proposal. Focused RED (missing `sealObservation`), GREEN, and subsequent focused/full uncached Go tests passed. Independent review found a further BLOCK: `.Perm()` masked special bits, permitting `02775` as `0775` with an inaccurate inverse. A second delegated strict-TDD correction reproduced the setuid/setgid/sticky cases RED, then refused all three special-bit pre-states while retaining the ordinary `0775` case GREEN; focused/full uncached Go suites passed.
- A pi-lens automatic reformat notification during verification interrupted the first follow-up review. No pre-change snapshot exists, so historical semantic equivalence is unknown. The parent captured the current test-file hash; a fresh independent **PASS** reviewed the current final tree with matching START/END SHA-256 hashes on `plan.go`, `change_plan.go`, `change_plan_test.go`, the change-plan document, and this tracker (before this progress update). Focused `go test -count=1 ./internal/deploy/homebrew`, full `go test -count=1 ./...`, tracked `git diff --check`, and read-only whitespace checks on the three untracked authored files all passed without diagnostics. The PASS covers the non-authorizing pure planner, not live filesystem continuity, service-manager evidence, apply, recovery execution, or device state; unit transition remains blocked. No commit yet; PLAN-3 stays unchecked until a coherent authorized work-unit commit and its boundary assessment are recorded. Generated `.codegraph/` must remain excluded.

## Next step

PLAN-3 code/tests/docs have an independent current-tree PASS but remain uncommitted. Confirm the exact candidate/staging scope, obtain explicit commit authorization if needed, commit one coherent work unit, record its identity and native candidate assessment, then check PLAN-3 complete. Do not begin TRANSACTION-1 or SERVICE-1 before this boundary. No remote/device operation, apply, activation, publication, or PR is authorized by this handoff.
